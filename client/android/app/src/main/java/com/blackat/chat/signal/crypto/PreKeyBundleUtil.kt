package com.blackat.chat.signal.crypto

import com.blackat.chat.data.model.Address
import com.blackat.chat.data.repository.SignalRepository
import org.signal.libsignal.protocol.SignalProtocolAddress
import org.signal.libsignal.protocol.state.PreKeyBundle

class PreKeyBundleUtil {
    companion object {
//        suspend fun getPrekeyBundle(address: SignalProtocolAddress) : PreKeyBundle {
//            val registrationId = SignalRepository.account().getRegistrationId()
//            val preKeyId = SignalRepository.signalPreKey().
//
//            return PreKeyBundle()
//        }
    }
}